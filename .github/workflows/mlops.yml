name: MLFlow and Snakemake Workflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  MLFLOW_URL: https://4af2-160-176-233-39.ngrok-free.app

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'

      - name: Set up virtual environment and install dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install mlflow prometheus_client snakemake pulp==2.7.0 jq
        shell: bash

      - name: Start Prometheus PushGateway
        run: |
          docker run -d -p 9091:9091 prom/pushgateway
          sleep 5
        shell: bash

      - name: Fetch and push MLFlow metrics to Prometheus
        run: |
          source venv/bin/activate
          echo "📡 Interrogation de MLFlow..."
          
          # Fetch the specific run IDs
          runs=("40861f35b8dc4f3082d2fcb92a9515c4" "ad0cf78265204f34b84a40aa09895c7f")
          for run in "${runs[@]}"; do
            echo "🔍 Analyse du run: $run"
            # Fetch the metrics for the specific run using get-history API
            metric_response=$(curl -s -X GET "$MLFLOW_URL/api/2.0/mlflow/metrics/get-history?run_id=$run&metric_key=scale")
            
            # Check if the response contains the correct metric data
            if echo "$metric_response" | jq -e '.metrics != null and (.metrics | length > 0)' > /dev/null; then
              scale_value=$(echo "$metric_response" | jq -r '.metrics[0].value')
              echo "Valeur de la métrique 'scale' pour le run $run : $scale_value"
              # Changer le nom de la métrique ici
              echo "MLFlow_and_Snakemake_Workflow{run=\"$run\"} $scale_value" \
                | curl --data-binary @- http://localhost:9091/metrics/job/mlflow_metrics
            else
              echo "⚠️ Aucune métrique 'scale' trouvée pour le run $run."
            fi
          done
        shell: bash

      - name: Push global success metric to Prometheus
        run: |
          echo "success 1" | curl --data-binary @- http://127.0.0.1:9091/metrics/job/mlflow_metrics
        shell: bash

      - name: Check metrics in PushGateway
        run: |
          echo "Vérification des métriques dans PushGateway..."
          if curl -s http://127.0.0.1:9091/metrics | grep -q 'MLFlow_and_Snakemake_Workflow'; then
            echo "✅ Les métriques sont présentes dans PushGateway."
          else
            echo "❌ Aucune métrique 'MLFlow_and_Snakemake_Workflow' trouvée dans PushGateway."
          fi
        shell: bash

      - name: Complete Monitoring
        run: echo "✅ Fin de l'exécution, vérification via Grafana et Prometheus"
        shell: bash
