name: MLFlow and Snakemake Workflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'

      - name: Set up virtual environment and install dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install mlflow prometheus_client snakemake pulp==2.7.0 jq
        shell: bash

      - name: Start MLFlow server
        run: |
          source venv/bin/activate
          nohup mlflow ui --host 0.0.0.0 --port 5000 &
          until curl -s http://localhost:5000 > /dev/null; do
            echo "📡 Attente que MLFlow soit prêt..."
            sleep 5
          done
          echo "✅ MLFlow est prêt."
        shell: bash

      - name: Start Prometheus PushGateway
        run: |
          docker run -d -p 9091:9091 prom/pushgateway
          sleep 5
        shell: bash

      - name: Inject test runs into experiments (if empty)
        run: |
          source venv/bin/activate
          echo "📥 Injection de runs tests dans MLflow (si nécessaire)..."
          experiments=$(curl -s -H "Content-Type: application/json" \
            -d '{"max_results": 100}' \
            "http://localhost:5000/api/2.0/mlflow/experiments/search" \
            | jq -r '.experiments[].experiment_id')

          for exp in $experiments; do
            runs=$(curl -s -H "Content-Type: application/json" \
              -d "{\"experiment_ids\": [\"$exp\"]}" \
              http://localhost:5000/api/2.0/mlflow/runs/search)

            if ! echo "$runs" | jq -e '.runs != null and (.runs | length > 0)' > /dev/null; then
              echo "🆕 Aucun run trouvé pour l'expérience $exp. Création d’un run test..."

              echo "
import mlflow
mlflow.set_experiment(experiment_id='$exp')
with mlflow.start_run():
    mlflow.log_param('auto_created', True)
    mlflow.log_metric('test_metric', 0.99)
              " > inject_run.py

              python inject_run.py
            else
              echo "✅ Des runs existent déjà pour l'expérience $exp."
            fi
          done
        shell: bash

      - name: Fetch and push MLFlow metrics to Prometheus
        run: |
          source venv/bin/activate
          echo "📡 Interrogation de MLFlow..."
          response=$(curl -s -H "Content-Type: application/json" \
            -d '{"max_results": 100}' \
            "http://localhost:5000/api/2.0/mlflow/experiments/search")

          if echo "$response" | jq -e '.experiments != null and (.experiments | length > 0)' > /dev/null; then
            echo "✅ Réponse valide de MLFlow :"
            echo "$response"

            experiments=$(echo "$response" | jq -r '.experiments[].experiment_id')

            for exp in $experiments; do
              echo "🔍 Expérience ID: $exp"
              runs=$(curl -s -H "Content-Type: application/json" \
                -d "{\"experiment_ids\": [\"$exp\"]}" \
                http://localhost:5000/api/2.0/mlflow/runs/search)

              if echo "$runs" | jq -e '.runs != null and (.runs | length > 0)' > /dev/null; then
                run_ids=$(echo "$runs" | jq -r '.runs[].info.run_id')

                for run in $run_ids; do
                  echo "📊 Analyse du run: $run"
                  metrics_response=$(curl -s http://localhost:5000/api/2.0/mlflow/runs/get?run_id=$run)

                  if echo "$metrics_response" | jq -e '.run.data.metrics != null and (.run.data.metrics | length > 0)' > /dev/null; then
                    metrics=$(echo "$metrics_response" | jq -r '.run.data.metrics')

                    echo "$metrics" | jq -c 'to_entries[]' | while read -r metric_entry; do
                      key=$(echo "$metric_entry" | jq -r '.key')
                      value=$(echo "$metric_entry" | jq -r '.value')
                      echo "Envoi de la métrique: $key{experiment=\"$exp\",run=\"$run\"} $value"
                      echo "$key{experiment=\"$exp\",run=\"$run\"} $value" \
                        | curl --data-binary @- http://localhost:9091/metrics/job/mlflow_metrics
                    done
                  else
                    echo "⚠️ Aucune métrique trouvée pour le run $run."
                  fi
                done
              else
                echo "⚠️ Aucun run trouvé pour l'expérience $exp."
              fi
            done
          else
            echo "❌ Aucune expérience trouvée dans MLFlow."
            echo "$response"
            exit 1
          fi
        shell: bash

      - name: Push global success metric to Prometheus
        run: |
          echo "success 1" | curl --data-binary @- http://127.0.0.1:9091/metrics/job/mlflow_metrics
        shell: bash

      - name: Check metrics in PushGateway
        run: |
          echo "Vérification des métriques dans PushGateway..."
          if curl -s http://127.0.0.1:9091/metrics | grep -q 'success'; then
            echo "✅ Les métriques sont présentes dans PushGateway."
          else
            echo "❌ Aucune métrique trouvée dans PushGateway."
          fi
        shell: bash

      - name: Complete Monitoring
        run: echo "✅ Fin de l'exécution, vérification via Grafana et Prometheus"
        shell: bash
